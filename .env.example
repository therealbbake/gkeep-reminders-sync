# Google Keep credentials
# Use either GKEEP_PASSWORD or GKEEP_MASTER_TOKEN (token-based login is often more reliable).
GKEEP_EMAIL=your-google-email@example.com
GKEEP_PASSWORD=
GKEEP_MASTER_TOKEN=

# Google Keep list title to read (unchecked items only)
GKEEP_LIST_TITLE=Groceries

# Apple iCloud credentials
# Consider using an app-specific password for APPLE_PASSWORD.
APPLE_ID=your-apple-id@example.com
APPLE_PASSWORD=

# Only needed on first run if iCloud prompts for 2FA/2SA.
# Set to the code you receive, then remove it after the session is trusted.
APPLE_2FA_CODE=

# Apple Reminders list to add items to (used if SYNC_LIST_NAMES is not set)
REMINDERS_LIST_NAME=Groceries

# To sync multiple lists by name (Keep and Reminders must match names), comma-separated
# Example: SYNC_LIST_NAMES=Groceries,Hardware,Pharmacy
SYNC_LIST_NAMES=

# Scheduling and logging
SCHEDULE_INTERVAL_MINUTES=5
LOG_LEVEL=INFO

# Where to persist iCloud cookies/sessions inside the container
ICLOUD_COOKIE_DIR=/data/icloud

# Optional timezone for the container (uncomment and adjust as needed)
# TZ=America/Chicago
